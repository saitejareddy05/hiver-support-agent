`golden_eval.csv` is a curated starter set for the demo repository. For a real
submission, run `python eval/build_golden_set.py` on the filtered brand data, then
replace the starter labels through the generated labeling worksheet. Human review
remains the source of truth for true_intent and true_should_escalate.
